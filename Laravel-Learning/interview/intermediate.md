# Interview Questions — Intermediate (After Phases 3–5)

1. **Explain the N+1 query problem. How do you fix it?**
2. **What's the difference between `with()` and `load()`?**
3. **Explain the difference between `hasMany`, `belongsTo`, `belongsToMany`, `hasManyThrough`, and polymorphic relationships.**
4. **What is mass assignment? Why is it dangerous? How do you prevent it in Laravel?**
5. **What is `$fillable` vs `$guarded`?**
6. **Explain Eloquent scopes. Give an example.**
7. **What are accessors and mutators?**
8. **What's an Eloquent observer?**
9. **What's the difference between `find()`, `findOrFail()`, and `first()`?**
10. **How does Laravel's Service Container work?**
11. **What's the difference between `bind` and `singleton`?**
12. **What's a Service Provider? Give an example use.**
13. **What is a Facade? Is it the same as a static method? How is it tested?**
14. **What are Events and Listeners? Why use them?**
15. **What are Queues used for? Name 3 queue drivers.**
16. **How do you make a Job run async?**
17. **What's the difference between Sanctum and Passport?**
18. **What's a Policy? Where would you use one over a Gate?**
19. **What is an API Resource? Why use one instead of returning an Eloquent model?**
20. **How does pagination work in API responses?**
21. **How do you cache an expensive query?**
22. **Explain transactions and when to use them.**
23. **What's `lockForUpdate()` for?**
24. **What's the difference between `->get()` and `->cursor()` for big datasets?**
25. **What does `php artisan migrate:fresh` do? Why would you use it instead of `migrate`?**
26. **How does Laravel handle cron jobs?**
27. **How do you send an email in Laravel?**
28. **What's a Notification? How is it different from a Mailable?**
29. **How do you upload a file in Laravel and store it on S3?**
30. **What's the difference between `Cache::remember` and `Cache::forever`?**

(Answers: re-read the relevant chapters. Force yourself to articulate from memory first.)
